#Disable batch job's auto start
spring.batch.job.enabled: false
logging:
  level:
    org.processor: DEBUG

batchJob:
  input: input-test
  output: output-test
  error: errorFiles-test
  cron: 0 */10 * * * ?